package org.example;

import org.example.data.DAO.ClientDAO;
import org.example.data.DAO.EmployeeDAO;
import org.example.entity.Employee;

public class Main {
    public static void main(String[] args) {
        //EmployeeDAO employeeDAO = new EmployeeDAO();
        //Employee employee = new Employee((long)298, "Andres", "Cardona", (long)123456789, 4, 4);
        //employeeDAO.deleteEmployee(employee);
        //employeeDAO.updateEmployee(employee);
        //employeeDAO.updateEmployee(employee);
        //employeeDAO.getAllEmployees().forEach(employee1 -> System.out.println(employee1));


        //ClientDAO clientDAO = new ClientDAO();
        //clientDAO.getAllClients().forEach(e-> System.out.println(e));

    }
}